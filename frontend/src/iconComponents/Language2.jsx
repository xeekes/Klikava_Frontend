import IconMask from "./IconMask";
import icon from "../assets/icons/language2.svg";

/**
 * Language icon for the language submenu in the header user menu.
 * @param {object} props
 */
const Language2 = (props) => <IconMask src={icon} {...props} />;
export default Language2;
